node_modules/
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
*.log

# Local runtime data — indexed chain data, keys, audit DB. Never commit.
/data/*.db
/data/*.db-*
/data/vault/
/data/*.local.json
/data/settings.json
/data/*.jsonl

# Rescue secrets must never be committed
*.key
secrets/

# Large richlist dumps (fetch locally; do not commit multi‑MB/GB files)
/datasets/balances-*.tsv
/datasets/balances-*.tsv.gz
/datasets/*.dat
/data/utxo*.dat
!/datasets/fixtures/

# Native grinder build artifacts (secp256k1 is cloned on first `make`)
# Glob covers every binary the Makefile emits, so a new one is ignored by default.
/native/grinder/satoshi-*
/native/grinder/*.o
/native/grinder/*.d
/native/grinder/third_party/
